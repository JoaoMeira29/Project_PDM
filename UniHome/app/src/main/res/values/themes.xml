<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UniHome" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.UniHome" parent="Base.Theme.UniHome" >
        <item name="android:statusBarColor">@color/keppel</item>
    </style>

    <style name="SwitchStyle">
        <item name="colorControlActivated">@color/keppel</item>
        <item name="colorSwitchThumbNormal">@color/white</item>
    </style>

    <style name="DateTimePickerStyle" >
        <item name="android:textColorPrimary">@color/lightBrown</item>
        <item name="colorControlNormal">@color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="SpinnerStyle">
        <item name="android:colorForeground">@color/keppel</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/keppel</item>
    </style>
</resources>